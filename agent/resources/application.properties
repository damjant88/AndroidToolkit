# AndroidToolkit Agent Configuration
spring.application.name=androidtoolkit-agent
server.port=8081

# WebSocket connection to Server
agent.server-url=ws://localhost:8080/ws/agent

# Allow large APK uploads
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB
